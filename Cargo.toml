[workspace]
members = ["dprint_plugin", "markup_fmt"]
resolver = "3"

[workspace.dependencies]
anyhow = "1.0"
insta = { version = "1.47", features = ["glob"] }
similar-asserts = "2.0"
toml = "1.1"

[profile.release]
lto = true
opt-level = "s"
strip = "debuginfo"
